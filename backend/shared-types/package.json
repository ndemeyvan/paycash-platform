{
  "name": "@paycash/shared-types",
  "version": "1.0.0",
  "description": "Shared types, interfaces, DTOs and constants for PayCash Platform",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "private": true,
  "scripts": {
    "build": "tsc",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "class-validator": "^0.14.1",
    "class-transformer": "^0.5.1"
  }
}
