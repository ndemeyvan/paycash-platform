{
  "name": "@paycash/api-gateway",
  "version": "1.0.0",
  "description": "API Gateway for PayCash Platform",
  "private": true,
  "scripts": {
    "start": "nest start",
    "start:dev": "nest start --watch",
    "build": "nest build",
    "test": "jest",
    "test:cov": "jest --coverage",
    "lint": "eslint src --ext .ts"
  }
}
